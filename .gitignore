# SSH Keys (SENSITIVE - never commit!)
keys/
*.key
*_key
*_key.pub

# Project-local SSH known hosts (contains target fingerprints)
.known_hosts

# Generated credentials (SENSITIVE - never commit!)
credentials/

# Client preparation package (contains SSH public key)
client-prep/
wazuh-client-prep.tar.gz
wazuh-client-prep.sh

# Generated configuration files (may contain passwords/secrets)
inventory/hosts.yml
group_vars/all.yml
group_vars/vault.yml
group_vars/all/main.yml
group_vars/all/vault.yml
ansible.cfg

# Generated maintenance scripts
scripts/run-scheduled-backup.sh

# Generated certificates (keep directory structure, ignore cert files)
files/certs/*.pem
files/certs/*.crt
files/certs/*.csr
files/certs/*.srl
files/certs/backup/
/certs/
*.crt
*.csr
*.srl

# Ansible runtime files
*.retry
.ansible_facts_cache/
/tmp/ansible_facts_cache/

# Ansible vault password file
.vault_pass
.vault_password
vault_password_file

# IDE and Editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Logs and temporary files
*.log
logs/
tmp/
temp/
*.tmp
*.bak
*.backup

# Backups created during deployment
backups/

# Rollback points
rollback-points/

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env
.venv/

# Claude Code working files
CLAUDE.md
improvements.md
.claude/
